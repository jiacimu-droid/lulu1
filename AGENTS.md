# Lulu1 修改代码必读

本文件是仓库的开发规则入口。开始修改前必须先读本文件，再读代码索引和目标模块代码。

## 1. 产品目标

露露机是生活在手机里的长期 AI 伴侣，不是普通聊天工具或通用问答助手。

- 核心目标是真实陪伴感、关系连续性、时间感、记忆和主动互动。
- 必须支持不同角色的人设与说话方式，不能把所有角色写成同一种语气。
- 角色在私聊、群聊、电话、游戏、日记、朋友圈等场景中应保持同一人设与关系状态。
- 模型上下文需要按功能正确接入角色设定、世界书、辞海、记忆、共同时间线和当前场景；不能只在页面上展示而不真正生效。
- 角色应像具体的人一样自然回应，避免解释性模板、机械总结、无缘由跳戏和统一客服腔。

## 2. 规则优先级

1. 主人在当前对话中最新、明确的要求。
2. 本文件 `AGENTS.md`。
3. 当前正式代码、数据结构与路由。
4. `docs/DESIGN_SYSTEM.md`、`docs/PROVENANCE.md` 等专项文档。
5. README、旧仓库、旧提交和历史说明仅作参考。

发现旧文档与最新要求冲突时，以最新要求为准；不得根据旧仓库或旧文档擅自恢复已经删除的功能。

## 3. 修改前必做

1. 阅读 `AGENTS.md`。
2. 阅读 `docs/architecture/CODE_INDEX.md`，先确认目标文件、行数和相关入口。
3. 查正式路由与调用点，确认修改的是当前实际使用的实现。
4. 阅读本次需求涉及的完整链路，包括 UI、Store/Repository、持久化、模型上下文、通知、权限和旧数据兼容。
5. 先规划相关文件，再集中修改，减少重复搜索和碎片提交。

## 4. 代码索引必须同步

- 新建、删除、移动、重命名或修改 `app/`、`scripts/`、`preview/` 中的代码与文本文件后，必须同步更新代码索引。
- 使用现有 `scripts/generate_code_index.py` 生成：
  - `docs/architecture/CODE_INDEX.md`
  - `docs/architecture/code-index.json`
- 不得只手工修改索引摘要，也不得让最终提交中的索引长期落后于源码。
- 只修改不参与索引的说明文档时，可以跳过索引更新。

## 5. 仓库边界

- 只修改 `jiacimu-droid/lulu1` 的现有 `main` 分支。
- 不新建分支，不开 PR。
- 不新建一次性或额外工作流。
- 保留并使用仓库现有的构建、发布和代码索引流程。
- 不顺手修改与当前需求无关的功能。
- 不提交密钥、Token、私有地址或用户隐私数据。

## 6. 功能与数据原则

- 页面能打开但数据不能保存，不算完成。
- UI、业务逻辑、持久化、失败状态、重启恢复和旧数据兼容需要一起检查。
- 删除功能时要清理入口、路由、运行时逻辑、模型上下文和残留展示；需要保留旧数据时，只保留必要兼容迁移。
- 用户明确要求保留的数据不得因为页面重做或文件拆分而丢失。
- 私聊与群聊是正式聊天类型；番茄钟陪伴、角色活动等进入正常会话，不随意创造额外聊天类型。
- 大文件按真实职责拆分，优先拆独立组件、解析器、Codec 和服务；不为追求行数机械切割。

## 7. 页面风格

- 以正式桌面 `MigrationDesktopScreenV2.kt` 为全局视觉基准。
- 整体以白色和中性灰为主，清楚、克制、轻量，具有手机桌面感和编辑排版感。
- 页面可使用模块专属的低饱和辅助色，但结构、留白、边框、圆角和文字层级应与桌面协调。
- 不再默认使用暖纸、麦黄色方案。
- 页面设计默认不放罗嗦的解释性文字、教学式小字、重复说明或为了填满空间而加的文案；尤其是设置、管理、工具类页面应以操作控件和必要信息为主。只有错误提示、必要约束、关键状态或用户明确要求时才保留说明文字。
- 可以参考 QQ、微信等产品的信息结构和操作习惯，但不复制其图标、资源或像素布局。
- 必须考虑窄屏、长屏、字体放大、状态栏、导航栏和键盘安全区。

详细规范见 `docs/DESIGN_SYSTEM.md`。

## 8. 验收与汇报

- 静态代码检查不能冒充 APK 构建或真机验收。
- 能确认构建结果时如实说明；无法确认时明确标记“待确认”。
- 长任务分阶段汇报进度，避免长时间没有反馈。
- 完成后只汇报真实改动、兼容影响、提交 SHA 和尚未确认的事项。