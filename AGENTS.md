# Lulu1 修改代码必读

本文件是仓库当前唯一的开发规则入口。修改代码前先读本文件，再查代码索引和目标模块；不需要把历史迁移文档重新读一遍。

## 规则优先级

1. 主人在当前对话中最新、明确的要求。
2. 本文件 `AGENTS.md`。
3. 当前实际代码、数据结构与正式路由。
4. `docs/DESIGN_SYSTEM.md`、`docs/PROVENANCE.md` 等专项文档。
5. README、旧提交和历史说明只能作为参考，不能覆盖最新要求。

发现文档与主人最新要求冲突时，以最新要求为准，并同步修正文档，不能继续照旧文档实现。

## 仓库与提交边界

- 目标仓库：`jiacimu-droid/lulu1`。
- 只修改现有 `main` 分支。
- 不新建分支，不开 PR。
- 不新建一次性或额外工作流。
- 保留仓库已有的正式构建、发布和代码索引流程。
- 不修改与当前需求无关的功能，不顺手扩大范围。
- 中间纯文档或准备提交可使用 `[skip ci] [skip index]`；最终代码提交应触发现有构建流程。

## 每次修改前的最短流程

1. 阅读本文件。
2. 阅读 `docs/architecture/CODE_INDEX.md`，确认相关文件、行数和入口。
3. 查正式路由和调用点，确认目标文件仍在实际使用，避免修改废弃页面。
4. 读取本次需求涉及的完整数据链路：UI、Store/Repository、持久化、通知、权限和兼容迁移。
5. 一次规划好需要修改的文件，尽量减少重复搜索和碎片提交。

不要求每次把整个仓库或所有说明文档重读一遍；只读取本文件、索引和当前模块的真实代码。

## 功能修改原则

- 页面能打开但数据不能保存，不算完成。
- 改 UI 时不得忽略真实业务、旧数据兼容、失败状态和重启后的持久化。
- 删除功能时要同时清理入口、路由、运行时 API、上下文注入和残留展示；需要保留旧数据时，只保留一次性兼容迁移。
- 角色相关功能必须保持人设、记忆、世界书、辞海、时间线和模型设置的一致性。
- 私聊与群聊是聊天系统的正式类型；番茄钟对话、角色活动通知等进入普通私聊，不建立额外聊天类型。
- 用户明确要求保留的旧数据不得因为页面重做而丢失。
- 不根据旧仓库或旧文档擅自恢复已删除功能。

## 页面设计规则

- 当前桌面 `MigrationDesktopScreenV2.kt` 是全局视觉基准。
- 设计语言以白色和中性灰为主，清楚、克制、轻量，带手机桌面与编辑排版感。
- 页面可以有模块专属辅助色，但整体结构、留白、边框、圆角和文字层级要与桌面协调。
- 不再默认使用“暖纸、麦黄色”设计。
- 不要求所有页面完全同色；健康、学习、通话等模块可以拥有低饱和主题色。
- 不复制 QQ、微信或其他产品的具体图标与像素布局；可以参考信息结构和操作习惯。
- 优先保证窄屏、长屏、字体放大、状态栏、导航栏和键盘安全区。

详细视觉规范见 `docs/DESIGN_SYSTEM.md`。

## 大文件与重构

以下类型的文件修改风险较高：聊天详情、电话页面、领域 Store、考研 Store、健康数据库解析和大型游戏页面。

- 修改大文件前先定位具体符号和调用点，不要无目的全文重写。
- 新增明显独立的 UI 卡片、解析器、Codec 或业务服务时，优先拆到同包小文件。
- 不为追求行数而机械拆分；拆分后必须保持状态单一来源和公开 API 清晰。
- 同一路径的读取与写入必须串行，写入前重新确认最新 SHA，避免覆盖主分支上的并发提交。

## 来源与安全

- 不直接整仓复制旧项目。
- 第三方代码、图片、字体、声音和模型资源必须确认来源与许可。
- 来源规则见 `docs/PROVENANCE.md`。
- 不提交密钥、Token、私有地址或用户隐私数据。

## 验收与汇报

- 代码检查只能证明静态链路，不能冒充真机验收。
- 能确认构建结果时如实说明；构建仍在进行或工具无法读取时，明确写“待确认”。
- 完成后只汇报本次真正改动、兼容影响、提交 SHA 和尚未确认的事项。
- 长任务分阶段告知进度，避免长时间无反馈。