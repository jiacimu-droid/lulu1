# Lulu 功能对等迁移清单

## 文档解释优先级

1. `docs/MIGRATION_PLAN.md` 是迁移范围与页面取舍的最终标准。
2. 计划明确写“原原本本迁移／功能迁入／保留”的能力，必须完整迁入，不能用占位页或自行重做的简化版替代。
3. 计划明确写“重写”的页面，只重写视觉与组织结构，旧业务能力、数据和失败路径仍须迁入。
4. 计划明确写“空页面／暂不迁入／删除／隐藏”的内容，必须按计划保持为空或移除，不得为了追求表面完整擅自恢复。
5. 其他对等文档与计划冲突时，以主人最新确认和 `MIGRATION_PLAN.md` 为准。

## 硬性原则

1. `lulu/master` 是计划内功能的来源基线，`lulu1/main` 必须达到计划内功能零丢失。
2. UI、导航和页面视觉可以按计划重做，但任何被计划要求保留的业务能力、数据、设置、玩法、历史记录、角色交互与失败路径都不能擅自删除。
3. 新增功能只能作为增量保留，不能替代旧功能，也不能填充计划明确要求留空的页面。
4. 某个 App 只有在旧功能逐项核对、数据迁移、入口接入、主要交互可用且构建通过后，才能标记为“完整迁移”。
5. 未核对完成的模块必须标记为“部分迁移”或“待核对”，禁止使用“完整迁移”表述。

## 游戏 App 基线

来源：`lulu/master/app/src/main/java/me/rerere/rikkahub/ui/pages/game/` 及对应数据层。

| 旧功能 | lulu1 状态 | 已核对结果 |
|---|---|---|
| 信号追踪 | 功能对等 | 角色选择、3×3 网格、三枚信号、五次探测、真实计分、逐步路线、角色结算、共同记忆和动画回放均已接入 |
| 满分男 | 功能对等 | 用户与角色轮流描述、秘密评分、猜分、逐轮记录、角色按人设回应和共同记忆均已接入 |
| 轻量跑团 | 功能对等 | 自由文本行动、真实 d20 判定、角色共同主持、连续回合、主动收尾和记录均已接入 |
| 海龟汤 | 功能对等 | 固定题面与汤底、自由提问、角色严格按是／否／无关主持、揭底和问答记录均已接入 |
| 默契问答 | 功能对等 | 角色结合人设、记忆和辞海秘密选择，用户作答、比对、计分和记录均已接入 |
| 一起猜拳 | 功能对等 | 引擎先锁定双方出拳和真实胜负，再让角色按人设回应；结果和共同记忆均已保存 |
| 快艇骰子 | 功能对等 | 五骰、最多三掷、保留骰子、十三类计分表、一次性记分和最终记录均已接入 |
| 五子棋 | 功能对等 | 15×15 棋盘、用户执黑先手、角色优先取胜／拦截／评估进攻、胜负记录和角色结算均已接入 |
| 游戏记录 | 功能对等 | 持久化保存角色、时间、得分、奖励、摘要、规则 JSON 和角色回应 |
| 游戏回放 | 部分迁移 | 信号追踪具备逐步动画回放；其他玩法目前展示完整规则 JSON 与角色回应，但尚未全部恢复为可逐步播放的专用回放界面 |

## 新增游戏（不得替代旧游戏）

- 记忆配对
- 心情猜猜看

## 其他 App 对等要求

以下模块按 `MIGRATION_PLAN.md` 的保留、重写、留空、删除规则逐项核对：

- 聊天与消息树
- 角色与角色设置
- 记忆
- 辞海
- 世界书
- 性能监测与 Token 控制台
- 阅读/共读
- 心愿馆
- 考研
- 游戏
- 通话与语音
- 朋友圈
- 设置、数据导入导出、权限、更新与备份
- 主动消息、提醒、承诺、责任与监督
- 插件及数字生活功能

## 验收状态定义

- **未迁移**：新仓库没有计划要求的该能力。
- **部分迁移**：已有入口或部分规则，但不具备计划要求的完整能力。
- **功能对等**：计划要求保留的旧功能、主要数据、入口和交互已保留。
- **增强完成**：在功能对等基础上增加新能力，且没有填充计划明确要求留空的内容。
